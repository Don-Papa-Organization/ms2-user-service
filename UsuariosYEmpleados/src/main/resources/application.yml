spring:
  application:
    name: UsuariosYEmpleados

  # ========== DATASOURCE (MySQL) ==========
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:don_papa}?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:MiContraseñaSegura123!}
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      connection-timeout: ${DB_CONNECTION_TIMEOUT:30000}
      maximum-pool-size: ${DB_MAX_POOL_SIZE:10}
      minimum-idle: ${DB_MIN_IDLE:5}
      initialization-fail-timeout: ${DB_INIT_FAIL_TIMEOUT:-1}
      connection-test-query: ${DB_CONNECTION_TEST_QUERY:SELECT 1}

  # ========== JPA ==========
  jpa:
    hibernate:
      ddl-auto: ${JPA_DDL_AUTO:update}
    show-sql: ${JPA_SHOW_SQL:true}
    properties:
      hibernate:
        dialect: ${HIBERNATE_DIALECT:org.hibernate.dialect.MySQL8Dialect}
        jdbc:
          batch_size: 20

  # ========== SERVER ==========
  server:
    port: ${PORT:8080}  # ¡IMPORTANTE! Render/Azure usan PORT

  # ========== ACTUATOR ==========
  management:
    endpoints:
      web:
        exposure:
          include: health,info
    endpoint:
      health:
        show-details: always
        probes:
          enabled: true

# ========== LOGGING ==========
logging:
  level:
    root: ${LOG_LEVEL:INFO}
    org.springframework.web: INFO
    com.zaxxer.hikari: DEBUG

# Secrets con valores predeterminados seguros (reemplazar en despliegue)
JWT_SECRET: ${JWT_SECRET:change-me-access-secret}
JWT_REFRESH_SECRET: ${JWT_REFRESH_SECRET:change-me-refresh-secret}

# ========== SERVER ==========
server:
  port: ${SERVER_PORT:${PORT:4002}}

# =============================================
# PERFIL: producción (opcional, SQL Server en nube)
# =============================================
---
spring:
  config:
    activate:
      on-profile: production
  datasource:
    url: jdbc:sqlserver://${DB_HOST}:${DB_PORT};database=${DB_NAME};encrypt=true;trustServerCertificate=false;hostNameInCertificate=*.database.windows.net;loginTimeout=30
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
    hikari:
      connection-timeout: ${DB_CONNECTION_TIMEOUT:30000}
      maximum-pool-size: ${DB_MAX_POOL_SIZE:10}
      minimum-idle: ${DB_MIN_IDLE:5}
      initialization-fail-timeout: ${DB_INIT_FAIL_TIMEOUT:-1}
      connection-test-query: ${DB_CONNECTION_TEST_QUERY:SELECT 1}
  jpa:
    hibernate:
      ddl-auto: ${JPA_DDL_AUTO:update}
    show-sql: ${JPA_SHOW_SQL:false}
    properties:
      hibernate:
        dialect: org.hibernate.dialect.SQLServerDialect
        format_sql: false
        hbm2ddl:
          auto: ${HIBERNATE_HBM2DDL_AUTO:update}

logging:
  level:
    root: INFO
    com.zaxxer.hikari: INFO